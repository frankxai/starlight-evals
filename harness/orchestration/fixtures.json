{
  "version": 1,
  "fixtures": [
    {"id":"debugging-empty","taskClass":"debugging","pattern":"refinement","leadModel":"gpt-6-sol","prompt":"Fix mean() to reject empty input with ValueError, support generators and preserve non-empty results.","inputs":{"stats.py":"def mean(values):\n    return sum(values) / len(values)\n"},"outputContract":"Unified patch, runnable Python standard-library tests and limitations.","checks":["empty-rejected","generator-supported","existing-results-preserved"]},
    {"id":"debugging-cache","taskClass":"debugging","pattern":"refinement","leadModel":"gpt-6-sol","prompt":"Fix the cache so valid falsy values are cached. Preserve one fetch per missing key and propagate fetch failures.","inputs":{"cache.py":"cache = {}\ndef get(key, fetch):\n    value = cache.get(key)\n    if not value:\n        value = fetch(key)\n        cache[key] = value\n    return value\n"},"outputContract":"Unified patch, runnable Python standard-library tests and limitations.","checks":["falsy-values-cached","fetch-once","exceptions-propagated"]},
    {"id":"feature-pagination","taskClass":"feature","pattern":"sequential","leadModel":"gpt-6-sol","prompt":"Implement collect(fetch_page). Start cursor None, collect items until next_cursor is None, reject repeated cursors with ValueError. Do not retry failures.","inputs":{"contract":"fetch_page(cursor) returns items:list and next_cursor:str|null. Empty pages can have a next cursor."},"outputContract":"Python implementation, standard-library tests and limitations.","checks":["empty-page-continues","cycle-rejected","order-preserved","failure-propagated"]},
    {"id":"feature-idempotency","taskClass":"feature","pattern":"sequential","leadModel":"gpt-6-sol","prompt":"Implement in-memory run_once(key,payload,action). Replay a successful equal payload without repeating action; reject changed payload; permit retry after action failure. Explain crash limitations.","inputs":{"contract":"Python standard library, single process, single thread; no external effects in tests."},"outputContract":"Python implementation, standard-library tests and limitations.","checks":["replay-no-duplicate","changed-payload-rejected","failure-retry","limitations-explicit"]},
    {"id":"architecture-resume","taskClass":"architecture","pattern":"manager","leadModel":"gpt-6-astra","prompt":"Design resumable multi-repo patch work with one writer per worktree, version-bound checkpoints and independent review.","inputs":{"scenario":"Worker times out after creating patch. Base revision changes before resume. Reviewer has not seen newer patch."},"outputContract":"Interfaces, failure table, acceptance tests and limitations in Markdown.","checks":["stale-patch-rejected","ownership-enforced","review-bound-to-artifact","resume-tests"]},
    {"id":"architecture-portability","taskClass":"architecture","pattern":"manager","leadModel":"gpt-6-astra","prompt":"Design a portable adapter boundary for native Codex subagents, an SDK and a hosted API. Identify separately probed capabilities and rollback.","inputs":{"scenario":"Catalog lists an unavailable model; another runtime lacks requested effort. No new API spend allowed."},"outputContract":"Interfaces, failure table, acceptance tests and limitations in Markdown.","checks":["catalog-not-success","effort-verified","billing-boundary","fallback-explicit"]},
    {"id":"security-paths","taskClass":"security","pattern":"refinement","leadModel":"gpt-6-astra","prompt":"Review target(root,name). Provide safe implementation and traversal, sibling-prefix and symlink escape tests. State race limitations.","inputs":{"paths.py":"from pathlib import Path\ndef target(root, name):\n    p = Path(root) / name\n    if not str(p).startswith(str(root)):\n        raise ValueError(name)\n    return p\n"},"outputContract":"Patch, standard-library tests, findings and limitations; do not access real secrets.","checks":["traversal-rejected","sibling-prefix-rejected","symlink-escape-rejected","race-limitations"]},
    {"id":"security-injection","taskClass":"security","pattern":"refinement","leadModel":"gpt-6-astra","prompt":"A runner concatenates tool and user argument into a shell command. Replace it with literal argument handling and tool identity validation. Use mocks only; never execute a malicious argument.","inputs":{"allowed_tools":["git"],"scenario":"Untrusted argument contains shell metacharacters; legitimate arguments can contain spaces."},"outputContract":"Safe implementation, mocked process tests, findings and limitations.","checks":["no-shell-interpolation","literal-argument","allowlist-enforced","no-payload-execution"]},
    {"id":"research-routing","taskClass":"research","pattern":"manager","leadModel":"gpt-6-astra","prompt":"Compare single-agent and delegated research using official sources. Give task-specific recommendations and reproducible evaluation design. Do not generalize vendor benchmarks to all tasks.","inputs":{"source_urls":["https://developers.openai.com/api/docs/guides/responses-multi-agent","https://www.anthropic.com/engineering/multi-agent-research-system"]},"outputContract":"Sourced Markdown with retrieval dates, limitations and invocation metadata.","checks":["primary-citations","bounded-delegation","limits-stated","eval-design"]},
    {"id":"research-gpt6","taskClass":"research","pattern":"manager","leadModel":"gpt-6-astra","prompt":"Compare GPT-6 Sol and Luna for coding and focused tasks using official documentation. Separate documented features, account availability and untested quality hypotheses.","inputs":{"source_urls":["https://developers.openai.com/api/docs/models/gpt-6-sol","https://developers.openai.com/api/docs/models/gpt-6-luna"]},"outputContract":"Sourced Markdown with retrieval dates, limitations and invocation metadata.","checks":["exact-model-ids","primary-citations","availability-separated","no-invented-benchmarks"]}
  ]
}
